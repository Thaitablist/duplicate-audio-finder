# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environment (each machine builds its own)
.venv/
venv/

# Build artifacts (regenerated by PyInstaller / Inno Setup)
dist/
build/_work/
*.spec.bak

# OS cruft
.DS_Store
Thumbs.db

# Raw icon source is kept; generated icon.png/icns/ico are committed so the
# app has them without a build step. (Do NOT ignore src/dupaudio/assets/*)
